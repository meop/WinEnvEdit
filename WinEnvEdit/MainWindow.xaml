<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinEnvEdit.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinEnvEdit"
    xmlns:vm="using:WinEnvEdit.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinEnvEdit">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,*">
        <CommandBar Grid.Row="0" DefaultLabelPosition="Collapsed" OverflowButtonVisibility="Collapsed" Padding="0,8,0,8">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <AppBarButton Icon="Upload" ToolTipService.ToolTip="Export (Ctrl+E)" ToolTipService.Placement="Bottom" Command="{Binding BackupCommand}" />
                    <AppBarButton Icon="Download" ToolTipService.ToolTip="Import (Ctrl+I)" ToolTipService.Placement="Bottom" Command="{Binding RestoreCommand}" />
                    <AppBarButton Icon="Refresh" ToolTipService.ToolTip="Reload (Ctrl+R)" ToolTipService.Placement="Bottom" Command="{Binding ReloadCommand}" />
                    <AppBarButton Icon="Undo" ToolTipService.ToolTip="Reset (Ctrl+Z)" ToolTipService.Placement="Bottom" Command="{Binding ResetCommand}" />
                    <AppBarButton Icon="Save" ToolTipService.ToolTip="Save (Ctrl+S)" ToolTipService.Placement="Bottom" Command="{Binding SaveCommand}" />
                    <AppBarButton Icon="Add" ToolTipService.ToolTip="Add (Ctrl+N)" ToolTipService.Placement="Bottom" Command="{Binding AddCommand}" />
                    <AppBarButton ToolTipService.ToolTip="Elevate to Admin" ToolTipService.Placement="Bottom" Command="{Binding ElevateCommand}">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE1DB;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                </StackPanel>
            </CommandBar.Content>
            <AppBarSeparator />
            <AppBarButton Icon="List" ToolTipService.ToolTip="Expand All Paths (Ctrl+E)" ToolTipService.Placement="Bottom" Command="{Binding ExpandAllPathsCommand}" />
            <AppBarButton Icon="View" ToolTipService.ToolTip="Show Volatile Vars (Ctrl+H)" ToolTipService.Placement="Bottom" Command="{Binding ShowVolatileVariablesCommand}" />
            <AppBarButton Icon="Help" ToolTipService.ToolTip="About (F1)" ToolTipService.Placement="Bottom" Command="{Binding AboutCommand}" />
        </CommandBar>

        <Grid Grid.Row="1" ColumnDefinitions="*,*">
            <Border Grid.Column="0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock Grid.Row="0" Text="SYSTEM" Style="{StaticResource SubtitleTextBlockStyle}" Margin="16,12,16,8" />
                    <ListView Grid.Row="1" ItemsSource="{Binding SystemVariables.Variables}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="8"
                                        Padding="12">
                                    <Grid RowDefinitions="Auto,Auto">
                                        <TextBlock Grid.Row="0" Text="{Binding Name}"
                                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                                   FontWeight="SemiBold" />
                                        <TextBox Grid.Row="1" Text="{Binding Value, Mode=TwoWay}"
                                                 Margin="0,8,0,0"
                                                 IsReadOnly="{Binding IsLocked}"
                                                 TextWrapping="Wrap" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <Border Grid.Column="1">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock Grid.Row="0" Text="USER" Style="{StaticResource SubtitleTextBlockStyle}" Margin="16,12,16,8" />
                    <ListView Grid.Row="1" ItemsSource="{Binding UserVariables.Variables}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="8"
                                        Padding="12">
                                    <Grid RowDefinitions="Auto,Auto">
                                        <TextBlock Grid.Row="0" Text="{Binding Name}"
                                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                                   FontWeight="SemiBold" />
                                        <TextBox Grid.Row="1" Text="{Binding Value, Mode=TwoWay}"
                                                 Margin="0,8,0,0"
                                                 IsReadOnly="{Binding IsLocked}"
                                                 TextWrapping="Wrap" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
